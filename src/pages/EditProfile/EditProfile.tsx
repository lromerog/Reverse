import React from 'react';
import { Box, Typography } from '@mui/material';

const EditProfile: React.FC = () => (
  <Box sx={{ minHeight: '100vh', background: '#E5E5E5', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
    <Typography variant="h3">Edit Profile Screen</Typography>
  </Box>
);

export default EditProfile; 